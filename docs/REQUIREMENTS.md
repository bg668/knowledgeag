| 需求编号       | 需求名称                         | 需求类型          | 问题背景                                                              | 需求描述                                                                                          | 验收标准                                                                                                                       | 优先级 | 影响对象                              | 状态  | 备注                            |
| ---------- | ---------------------------- | ------------- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | --- | --------------------------------- | --- | ----------------------------- |
| REQ-KM-001 | Source 幂等导入与版本管理             | 数据一致性         | 当前同一文档多次导入会生成多个 source_id，version_id 相同但 source 重复                | 基于 `uri + content_hash` 或 `content_hash` 做幂等导入；同内容复用已有 source/version，不同内容生成新 version         | 1. 相同文档重复导入不产生重复 source；2. 同一 source 的不同版本可追踪；3. claim/evidence/card 均能回到正确 source_version                                 | P0  | sources、import 流程、version_id 生成逻辑 | 已完成；待人工核验 | 原 REQ-004                     |
| REQ-KM-002 | 支持 SourceSummary             | 知识预编译         | 当前 source_summary 不稳定，部分 source 为空，无法作为文档级预编译结果                   | 每个输入文档导入后生成稳定的 SourceSummary，概括文档主题、核心观点、适用场景和主要结构                                            | 1. 每个有效 source 都有 source_summary；2. summary 能覆盖全文主旨；3. summary 不替代 card，只作为文档级索引                                           | P0  | sources 表、导入流程、LLM 处理流程           | 已完成；待人工核验 | 新增                            |
| REQ-KM-003 | 支持主题化 KnowledgeCard          | 核心领域能力        | 当前 card 数量偏少，且没有稳定粒度规则；claim 不应成为主要阅读对象                           | 按主题生成 KnowledgeCard，每张 card 聚合 3–7 个关键点，可独立用于检索、阅读和复用                                         | 1. card 围绕单一明确主题；2. 每张 card 包含 3–7 个 core_points；3. 一篇长文可生成多张 card；4. card 不是简单摘要段落                                        | P0  | cards、card 生成 prompt、card schema  | 已完成；待人工核验 | 原 REQ-005，作为 REQ-KM-004 的设计约束 |
| REQ-KM-004 | 修复 Card-Claim-Evidence 绑定关系  | 数据质量 / 证据链    | 当前 card 的 core_points 与 claim_ids、evidence_ids 不完全一致，导致卡片正文和证据链脱节 | card 中每个核心点必须绑定对应 claim；每条 claim 必须绑定可支撑的 evidence；不允许无理由复用固定 claim 集合                        | 1. card.core_points 均可追溯到 claim；2. claim.evidence_ids 能支撑 claim；3. 不同 card 不应绑定完全相同 claim 集合；4. card、claim、evidence 三者关系一致 | P0  | cards、claims、evidences、生成逻辑       | 已完成；待人工核验 | 原 REQ-001                     |
| REQ-KM-005 | Evidence 拆分为 Quote + Context | 回源能力          | 当前 evidence 是较大的上下文窗口，有时从半句话开始，不够精确                               | Evidence 拆为最小证据引用和上下文：`evidence_quote`、`context_before`、`context_after`、`loc`                 | 1. evidence_quote 是最小可验证原文片段；2. context 只辅助阅读；3. loc 可定位章节和字符/行号；4. evidence 不应从截断半句开始                                     | P0  | evidences 表、evidence 抽取逻辑、回源展示    | 已完成；待人工核验 | 原 REQ-003                     |
| REQ-KM-006 | 基于结构的多 KnowledgeCard 组织      | 核心领域能力        | 文档通常包含标题、章节和主题块；如果默认压缩为唯一全文卡，会丢失可检索、可复用的主题结构                         | 从 Source 中识别标题、章节、主题块，生成多张 KnowledgeCard。默认不把整篇文档压缩成唯一卡片；总览卡可以存在，但不能替代主题卡       | 1. 能基于 Source 结构识别标题、章节、主题块；2. 长文默认生成多张主题卡；3. 总览卡存在时不替代主题卡；4. 不把整篇文档压缩成唯一卡片                                     | P0  | StructuralSplitter、CardOrganizer、生成 prompt | 已完成；待人工核验 | 替换原受控 Schema 需求                 |
| REQ-KM-007 | 关键主题覆盖校验                    | 质量评估          | 当前缺少对标题、关键术语和核心主题是否被卡片或 claim 覆盖的校验，容易遗漏重要内容                         | 从 Source 中提取标题、关键术语、核心主题，校验是否被 KnowledgeCard / Claim 覆盖。未覆盖内容输出 missing_topics，供人工或后续流程处理 | 1. 能提取 Source 的标题、关键术语和核心主题；2. 能校验主题是否被 KnowledgeCard / Claim 覆盖；3. 未覆盖内容输出为 missing_topics；4. missing_topics 可供人工或后续流程处理 | P0  | 导入流程、覆盖校验、质量报告                 | 已完成；待人工核验 | 替换原 SOPCard / PatternCard 需求      |
| REQ-KM-008 | KnowledgeCard 粒度控制           | 数据质量          | card 过粗会退化为全文总览，过碎会退化为句子卡，都会降低检索、阅读和复用质量                            | 控制单张卡片的信息密度。单卡应围绕一个明确主题，包含适量 claim/evidence；避免过粗的“全文总览卡”，也避免过碎的“句子卡”       | 1. 单张 card 围绕一个明确主题；2. 单张 card 包含适量 claim/evidence；3. 能过滤或修正过粗的全文总览卡；4. 能过滤或合并过碎的句子卡                         | P0  | CardOrganizer、CardValidator、生成 prompt | 已完成；待人工核验 | 替换原 SynthesisCard 需求             |
| REQ-KM-009 | 增加 Source 覆盖率检查              | 质量评估          | 当前无法判断一篇文档哪些章节已被整理，哪些内容被遗漏                                        | 每次导入后生成 coverage report，记录已覆盖章节、未覆盖章节、card 数、claim 数、每张 card 的 claim/evidence 数               | 1. 能看到 source 中哪些 section 被覆盖；2. 能识别未覆盖的重要章节；3. 支持 JSON 输出；4. 可用于后续测试评估                                                    | P1  | 导入流程、评估脚本                         | 已完成；待人工核验 | 原 REQ-002                     |
| REQ-KM-010 | 建立质量评估指标                     | 测试 / 评估       | 当前只能人工查看输出，无法持续判断整理质量是否变好                                         | 为每次导入生成质量指标，包括 card 数、claim 数、evidence 数、绑定完整率、覆盖率、重复 source 数、引用精确率                          | 1. 命令行可生成评估报告；2. 指标能发现明显错误；3. 支持与 expected output 对比；4. 可用于回归测试                                                            | P1  | tests、评估脚本、CI 可选                  | 已完成；待人工核验 | 原 REQ-006                     |
| REQ-KM-011 | 建立 test_doc.md 期望输出样例        | 测试基线          | 当前缺少标准答案，无法判断程序输出是否符合预期                                           | 为 test_doc.md 人工整理 expected_source、expected_cards、expected_claims、expected_evidences          | 1. expected 文件可用于自动测试；2. 包含合理 card 划分；3. 包含 claim/evidence 绑定；4. 可与程序输出做差异评估                                               | P1  | tests/fixtures、评估脚本               | 已完成；待人工核验 | 原 REQ-007                     |
| REQ-KM-012 | 支持代码开发知识卡片类型                 | 场景扩展          | 代码类知识库不能只存“文件说明”，还要存“变化会影响哪里”                                     | 增加 ProjectContext、ModuleCard、EntryPointCard、ChangeImpactCard、DecisionRecord 等代码开发知识对象         | 1. ProjectContext 可描述项目地图；2. ModuleCard 可描述模块输入输出和依赖；3. ChangeImpactCard 可描述修改影响面；4. DecisionRecord 可记录设计取舍                | P2  | schema、card_type、生成 prompt、检索策略   | 已完成；待人工核验 | 用于 AI Coding 场景；第一版复用 KnowledgeCard 通用 schema，通过 card_type 扩展完成 |
| REQ-KM-013 | 支持金融知识卡片类型                   | 场景扩展          | 金融知识中事实、观点、假设、策略、复盘容易混在一起                                         | 增加 FactCard、EventCard、ThesisCard、StrategyCard、ReviewCard，区分事实、解释、判断、策略和复盘                     | 1. FactCard 只记录事实和数据；2. ThesisCard 记录投资逻辑；3. StrategyCard 记录操作规则；4. ReviewCard 记录结果验证                                      | P2  | schema、card_type、生成 prompt、检索策略   | 已完成；待人工核验 | 用于金融知识管理；第一版复用 KnowledgeCard 通用 schema，通过 card_type 扩展完成 |
| REQ-KM-014 | 支持任务后复盘沉淀                    | 闭环能力          | 知识库不能只接收文档，还应沉淀任务执行经验                                             | 支持任务完成后由人工通过 `run_id` 触发复盘，从观测日志自动读取原任务、输入、输出、LLM 调用、质量指标和错误信息，沉淀为 ReviewCard、SOPCard、PatternCard | 1. `/review <run_id>` 可生成复盘卡片；2. Source URI 指向 `observability://runs/<run_id>`；3. 能关联运行输入、输出、LLM 调用、指标和产物；4. 后续任务前可检索复用 | P2  | agent 流程、memory/knowledge 写入流程、observability | 已完成；待人工核验 | 替代第一版 JSON 手动触发写入 |
| REQ-KM-015 | 将 LLM Adapter 收口为 KnowledgeAgent 接口 | 架构边界 / Agent 执行 | 当前 `BaseLLMAdapter` 名称偏向底层 LLM 适配器，但实际承担 `summarize_source`、`extract_claims`、`organize_cards`、`answer` 等知识任务；同时 paimonsdk 应作为默认执行器，而不是主流程控制器 | 将现有 LLM 基类调整为 `KnowledgeAgent`；knowledgeag 主流程仍自行控制导入、检索、存储、校验等流程；需要模型参与的节点通过 `KnowledgeAgent` 调用；默认实现为 `PaimonKnowledgeAgent`；`PaimonKnowledgeAgent` 支持多步执行，并可利用 paimonsdk 的工具注册能力，但注册哪些工具、是否允许工具调用、每个任务节点允许哪些工具，必须由 `KnowledgeAgent` 根据 knowledgeag 配置控制；工具本身由 knowledgeag 内部实现并注册到 `ToolRegistry`；不支持外部 skill 注册；第三方 provider 继续通过 `baseUrl`、`apiKeyEnv`、`model` 配置接入；如用户需要更复杂能力，可实现自己的 `KnowledgeAgent` 接口替换默认实现 | 1. 主流程不直接依赖 paimonsdk client 或 agent 实例；2. paimonsdk 仅作为默认 `KnowledgeAgent` 实现；3. `extract_claims`、`organize_cards`、`answer` 等节点均通过统一 `KnowledgeAgent` 接口调用；4. knowledgeag 内部实现 `ToolRegistry`，工具由 knowledgeag 提供；5. paimonsdk 只负责承接工具注册与执行，不决定工具范围；6. `PaimonKnowledgeAgent` 必须根据配置选择允许注册的内部工具；7. 支持按任务节点配置工具权限，例如 `extract_claims`、`organize_cards`、`answer` 可使用不同工具集；8. 支持单轮模式：`max_steps=1` 且 `allow_tools=false`，此时不得注册任何工具；9. 不支持外部 skill 注册；10. paimonsdk 初始化失败时不得静默 fallback 到 Mock；11. Mock 实现仍可用于测试；12. 外部用户可通过实现 `KnowledgeAgent` 接口接入自定义 Agent | P0  | adapters/llm、agents、tools、app/container、config、ingestion、answer_service | 待实现 | 核心边界：工具属于 knowledgeag，工具权限由 KnowledgeAgent 控制，paimonsdk 只是默认执行器 |
| REQ-KM-016 | 建立运行观测日志                    | 可观测性          | `/ingest`、`/ask` 运行过程缺少稳定 run 记录，事后难以定位 LLM 调用、指标和失败原因                         | 每次 `/ingest`、`/ask` 生成 `run_id`，写入独立观测 SQLite，记录 runs、llm_calls、run_artifacts、run_metrics；超大原文只保存路径、hash、长度、预览 | 1. 默认观测库路径为 `data/logs/knowledgeag_observability.sqlite3`；2. 观测库独立于知识库；3. LLM 调用记录节点、模型、prompt、裁剪输入、原始输出、显式 thinking、错误和耗时；4. 原文大内容不复制进日志库 | P1  | observability、config、AgentApp、KnowledgeAgent、ingest/ask 流程 | 已完成；待人工核验 | 长期全量保留观测日志，原文仅记录引用信息 |
| REQ-KM-017 | TUI 展示显式 LLM thinking           | 可观测性 / 交互    | TUI 只能看到最终输出或文本流，无法区分 provider 显式返回的 thinking 与普通输出                           | 扩展 LLM 事件流，TUI 在 `/ask` 和 `/ingest` 中展示当前 LLM 节点、显式 thinking、输出预览和最终结果，并提供 `/runs` 查看最近运行 | 1. 只展示 `ThinkingContent`、reasoning summary 等 provider 显式返回内容；2. 不展示隐藏推理；3. mock 后端可产生可测试事件；4. `/runs` 显示 run_id、命令、状态和时间 | P1  | agents、runtime/tui_app、observability | 已完成；待人工核验 | 第一版事件对象为轻量 `LLMEvent(kind, run_id, node, text, metadata)` |
