| 需求编号       | 需求名称                         | 需求类型          | 问题背景                                                              | 需求描述                                                                                          | 验收标准                                                                                                                       | 优先级 | 影响对象                              | 状态  | 备注                            |
| ---------- | ---------------------------- | ------------- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | --- | --------------------------------- | --- | ----------------------------- |
| REQ-KM-001 | Source 幂等导入与版本管理             | 数据一致性         | 当前同一文档多次导入会生成多个 source_id，version_id 相同但 source 重复                | 基于 `uri + content_hash` 或 `content_hash` 做幂等导入；同内容复用已有 source/version，不同内容生成新 version         | 1. 相同文档重复导入不产生重复 source；2. 同一 source 的不同版本可追踪；3. claim/evidence/card 均能回到正确 source_version                                 | P0  | sources、import 流程、version_id 生成逻辑 | 待实现 | 原 REQ-004                     |
| REQ-KM-002 | 支持 SourceSummary             | 知识预编译         | 当前 source_summary 不稳定，部分 source 为空，无法作为文档级预编译结果                   | 每个输入文档导入后生成稳定的 SourceSummary，概括文档主题、核心观点、适用场景和主要结构                                            | 1. 每个有效 source 都有 source_summary；2. summary 能覆盖全文主旨；3. summary 不替代 card，只作为文档级索引                                           | P0  | sources 表、导入流程、LLM 处理流程           | 待实现 | 新增                            |
| REQ-KM-003 | 支持主题化 KnowledgeCard          | 核心领域能力        | 当前 card 数量偏少，且没有稳定粒度规则；claim 不应成为主要阅读对象                           | 按主题生成 KnowledgeCard，每张 card 聚合 3–7 个关键点，可独立用于检索、阅读和复用                                         | 1. card 围绕单一明确主题；2. 每张 card 包含 3–7 个 core_points；3. 一篇长文可生成多张 card；4. card 不是简单摘要段落                                        | P0  | cards、card 生成 prompt、card schema  | 待实现 | 原 REQ-005，作为 REQ-KM-004 的设计约束 |
| REQ-KM-004 | 修复 Card-Claim-Evidence 绑定关系  | 数据质量 / 证据链    | 当前 card 的 core_points 与 claim_ids、evidence_ids 不完全一致，导致卡片正文和证据链脱节 | card 中每个核心点必须绑定对应 claim；每条 claim 必须绑定可支撑的 evidence；不允许无理由复用固定 claim 集合                        | 1. card.core_points 均可追溯到 claim；2. claim.evidence_ids 能支撑 claim；3. 不同 card 不应绑定完全相同 claim 集合；4. card、claim、evidence 三者关系一致 | P0  | cards、claims、evidences、生成逻辑       | 待实现 | 原 REQ-001                     |
| REQ-KM-005 | Evidence 拆分为 Quote + Context | 回源能力          | 当前 evidence 是较大的上下文窗口，有时从半句话开始，不够精确                               | Evidence 拆为最小证据引用和上下文：`evidence_quote`、`context_before`、`context_after`、`loc`                 | 1. evidence_quote 是最小可验证原文片段；2. context 只辅助阅读；3. loc 可定位章节和字符/行号；4. evidence 不应从截断半句开始                                     | P0  | evidences 表、evidence 抽取逻辑、回源展示    | 待实现 | 原 REQ-003                     |
| REQ-KM-006 | 建立受控 Schema 与关系类型            | 领域建模          | 自动知识图谱不能神化；模型自由生成关系会导致节点多、边多但不可控                                  | 定义受控 schema，包括 Source、Evidence、Claim、KnowledgeCard、SOPCard、PatternCard、SynthesisCard，以及受控关系类型 | 1. 关系类型由 schema 定义；2. 模型不得自由创造关系；3. 非法关系可被校验拦截；4. schema 文档可供 AI 和开发人员阅读                                                   | P0  | domain schema、数据校验、生成 prompt      | 待实现 | 新增，防止“自由造图”                   |
| REQ-KM-007 | 支持 SOPCard / PatternCard     | 使用场景建模        | 当前 card 只有通用知识卡片，无法区分流程、模式、原则等不同知识用途                              | 在 KnowledgeCard 基础上扩展 SOPCard 和 PatternCard，用于沉淀可执行流程与可迁移模式                                   | 1. SOPCard 包含触发条件、步骤、输入输出、验收方式；2. PatternCard 包含适用场景、解决问题、反模式、示例；3. 与普通 KnowledgeCard 可区分检索                                | P1  | cards、card_type、生成 prompt、检索逻辑    | 待实现 | 新增                            |
| REQ-KM-008 | 支持 SynthesisCard 高频综合页       | 知识预编译 / 多文档综合 | 高频复杂问题不应每次临时召回 chunk 后重新拼接推理                                      | 支持对高频问题、常见任务、跨文档主题提前生成 SynthesisCard，作为预编译综合页                                                 | 1. 可由多个 source/card/claim/evidence 生成；2. 记录输入来源；3. 能回答一个高频复杂问题；4. 支持后续更新和重新生成                                              | P1  | cards、synthesis 生成流程、检索流程         | 待实现 | 新增                            |
| REQ-KM-009 | 增加 Source 覆盖率检查              | 质量评估          | 当前无法判断一篇文档哪些章节已被整理，哪些内容被遗漏                                        | 每次导入后生成 coverage report，记录已覆盖章节、未覆盖章节、card 数、claim 数、每张 card 的 claim/evidence 数               | 1. 能看到 source 中哪些 section 被覆盖；2. 能识别未覆盖的重要章节；3. 支持 JSON 输出；4. 可用于后续测试评估                                                    | P1  | 导入流程、评估脚本                         | 待实现 | 原 REQ-002                     |
| REQ-KM-010 | 建立质量评估指标                     | 测试 / 评估       | 当前只能人工查看输出，无法持续判断整理质量是否变好                                         | 为每次导入生成质量指标，包括 card 数、claim 数、evidence 数、绑定完整率、覆盖率、重复 source 数、引用精确率                          | 1. 命令行可生成评估报告；2. 指标能发现明显错误；3. 支持与 expected output 对比；4. 可用于回归测试                                                            | P1  | tests、评估脚本、CI 可选                  | 待实现 | 原 REQ-006                     |
| REQ-KM-011 | 建立 test_doc.md 期望输出样例        | 测试基线          | 当前缺少标准答案，无法判断程序输出是否符合预期                                           | 为 test_doc.md 人工整理 expected_source、expected_cards、expected_claims、expected_evidences          | 1. expected 文件可用于自动测试；2. 包含合理 card 划分；3. 包含 claim/evidence 绑定；4. 可与程序输出做差异评估                                               | P1  | tests/fixtures、评估脚本               | 待实现 | 原 REQ-007                     |
| REQ-KM-012 | 支持代码开发知识卡片类型                 | 场景扩展          | 代码类知识库不能只存“文件说明”，还要存“变化会影响哪里”                                     | 增加 ProjectContext、ModuleCard、EntryPointCard、ChangeImpactCard、DecisionRecord 等代码开发知识对象         | 1. ProjectContext 可描述项目地图；2. ModuleCard 可描述模块输入输出和依赖；3. ChangeImpactCard 可描述修改影响面；4. DecisionRecord 可记录设计取舍                | P2  | schema、card_type、生成 prompt、检索策略   | 待规划 | 用于 AI Coding 场景               |
| REQ-KM-013 | 支持金融知识卡片类型                   | 场景扩展          | 金融知识中事实、观点、假设、策略、复盘容易混在一起                                         | 增加 FactCard、EventCard、ThesisCard、StrategyCard、ReviewCard，区分事实、解释、判断、策略和复盘                     | 1. FactCard 只记录事实和数据；2. ThesisCard 记录投资逻辑；3. StrategyCard 记录操作规则；4. ReviewCard 记录结果验证                                      | P2  | schema、card_type、生成 prompt、检索策略   | 待规划 | 用于金融知识管理                      |
| REQ-KM-014 | 支持任务后复盘沉淀                    | 闭环能力          | 知识库不能只接收文档，还应沉淀任务执行经验                                             | 支持任务完成后由人工触发，将成功经验、失败原因、修改过程沉淀为 ReviewCard、SOPCard、PatternCard                                | 1. 支持手动触发；2. 输出复盘卡片；3. 能关联原任务、输入、输出、修改文件或证据；4. 后续任务前可检索复用                                                                  | P2  | agent 流程、memory/knowledge 写入流程    | 待规划 | 对应长期 agent 经验库                |
