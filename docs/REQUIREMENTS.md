| 需求编号    | 需求名称                            | 需求类型        | 问题背景                                                                         | 需求描述                                                                                                               | 验收标准                                                                                                                                   | 优先级 | 影响对象                                | 状态  | 备注                                                                   |
| ------- | ------------------------------- | ----------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------- | --- | ----------------------------------- | --- | -------------------------------------------------------------------- |
| REQ-001 | 修复 Card 与 Claim / Evidence 绑定关系 | 数据质量 / 领域模型 | 当前 card 的 `core_points` 与绑定的 `claim_ids`、`evidence_ids` 不完全一致，导致知识卡片正文和证据链脱节 | 每张 card 中出现的核心观点，必须绑定对应 claim；每条 claim 必须绑定可支撑它的 evidence；card 的 `claim_ids` 和 `evidence_ids` 应由实际内容关系生成，而不是复用固定集合 | 1. 每张 card 至少绑定 3 条相关 claim；2. card.core_points 中的每个核心点都能追溯到 claim；3. claim.evidence_ids 均能支撑 claim 文本；4. 不同 card 不应无理由绑定完全相同 claim 集合 | P0  | cards、claims、evidences、card 生成逻辑    | 待实现 | 这是当前最关键问题                                                            |
| REQ-002 | 增加 Source 覆盖率检查                 | 质量评估 / 可观测性 | 当前无法判断一篇文档哪些章节已被整理，哪些内容被遗漏                                                   | 在每次导入 source 后生成覆盖率报告，记录已覆盖章节、未覆盖章节、生成 card 数、claim 数、每张 card 的 claim 数和 evidence 数                                | 1. 每次导入后输出 coverage report；2. 能看到 source 中哪些 section 被 card/claim 覆盖；3. 能识别未覆盖的重要章节；4. 支持后续作为测试评估指标                                    | P0  | source 处理流程、card 生成流程、评估脚本          | 待实现 | 可先做 JSON 输出，不必进数据库                                                   |
| REQ-003 | 拆分 Evidence 为 Quote + Context   | 回源能力 / 数据结构 | 当前 evidence 是较大的上下文窗口，不够精确，有时从半句话开始，无法作为严格证据引用                               | Evidence 需要区分最小证据引用和上下文窗口，建议增加 `evidence_quote`、`context_before`、`context_after`、`loc` 等结构                         | 1. evidence_quote 是能直接支撑 claim 的最小原文片段；2. context_before/context_after 只作为辅助阅读；3. loc 能定位到章节和字符/行号范围；4. evidence 不应从截断半句开始             | P0  | evidences 表、evidence 抽取逻辑、回源展示逻辑    | 待实现 | 影响后续可信度                                                              |
| REQ-004 | Source 幂等导入                     | 数据一致性 / 存储  | 当前同一个 test_doc.md 多次导入会生成多个 source_id，version_id 相同但 source 重复               | 对相同 `uri + version_id` 或 `content_hash` 的 source 做幂等处理；重复导入时复用已有 source，或显式创建新 version，而不是无控制插入重复 source           | 1. 相同内容重复导入不产生重复 source；2. source_id 与 version_id 关系清晰；3. 同一文档不同内容版本可追踪；4. claim/evidence 能稳定回到正确 source version                       | P0  | sources 表、import 流程、version_id 生成逻辑 | 待实现 | 建议优先使用 content_hash                                                  |
| REQ-005 | 明确 KnowledgeCard 的合理粒度          | 产品规则 / 领域建模 | 当前 card 数量偏少，不能覆盖完整文章；过多 claim 又会变成碎片化知识                                     | 定义 card 粒度规则：一张 card 应围绕一个明确主题，包含 3–7 个核心点，可独立用于检索、复用和指导行动                                                         | 1. 一篇长文可生成多张 card；2. 每张 card 有明确主题；3. card 不是简单摘要段落；4. card 可独立回答一个知识问题                                                                | P1  | card 生成 prompt、card schema、评估规则     | 待实现 | REQ-005 是 REQ-001 的设计约束，但 REQ-001 可先修复当前绑定错误；后续再用 REQ-005 优化 card 粒度 |
| REQ-006 | 增加导入结果质量评估指标                    | 测试 / 评估     | 当前只能人工查看输出，难以持续判断整理质量是否变好                                                    | 为每次导入生成质量指标，包括 card 数、claim 数、evidence 数、claim 覆盖率、card-claim 绑定完整率、evidence 精确率、重复 source 数                       | 1. 支持命令行生成评估报告；2. 支持与 expected output 对比；3. 能用于回归测试；4. 指标能暴露明显错误                                                                       | P1  | 测试脚本、评估脚本、CI 可选                     | 待实现 | 方便后续 AI 修改后验收                                                        |
| REQ-007 | 建立 test_doc.md 的期望输出样例          | 测试数据 / 验收基线 | 当前缺少标准答案，导致无法判断程序输出是否符合预期                                                    | 为 test_doc.md 人工整理一版 expected_source、expected_cards、expected_claims、expected_evidences，用于后续回归测试                    | 1. expected 输出文件可直接用于测试；2. 包含合理 card 划分；3. 包含 claim/evidence 绑定关系；4. 程序输出可与 expected 做差异评估                                             | P1  | tests、fixtures、评估脚本                 | 待实现 | 建议放入 `tests/fixtures/test_doc_expected.json`                         |






